import { Injectable } from '@angular/core';

// import { ApiClientService } from './util/api.client.service';

@Injectable()
export class PlanningService {
    constructor(
        // private http: ApiClientService
    ) { }

    // getServicePlanning(id: number) {
    //     return this.http.get('/secure/servicehasprofessional/' + id, { secure: false });
    // }

    // postServicePlanning(list: any[]) {
    //     return this.http.post('/planning', list, { asJson: false });
    // }

    // deleteServicePlanning(planning: any) {
    //     return this.http.delete('/planning/' + planning.id);
    // }
    // getMedicalPlanning(id: any) {
    //     return this.http.get("/planning/person/" + id);
    // }
    // getMedicalPlanningDate(serviceId: any, date: any) {
    //     return this.http.get("/schedule/pending/" + serviceId + "/" + date);
    // }
    // getServiceFreeSchedules(serviceId: any) {
    //     return this.http.get("/schedule/pending/service?serviceId="+ serviceId);
    // }
}